// Written by `weg secondary config bind`. Regenerate it rather than edit it.
// BIND 9.16 or newer, which is where `masters` became `primaries`.
// Include this file from named.conf.

key "ns2.example.com." {
    algorithm hmac-sha256;
    secret "aG93IG5vdyBicm93biBjb3c=";
};

server 192.0.2.1 {
    keys { "ns2.example.com."; };
};

zone "example.com." {
    type secondary;
    primaries { 192.0.2.1; };
    file "/var/lib/named/example.com.zone";
};

zone "2.0.192.in-addr.arpa." {
    type secondary;
    primaries { 192.0.2.1; };
    file "/var/lib/named/2.0.192.in-addr.arpa.zone";
};
