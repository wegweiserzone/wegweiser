<!--
The diff says how. This says what, and why.

From CONTRIBUTING.md, before you open it:

  - `make check` passes
  - every commit is signed off: `git commit -s`
  - anything somebody using the server, the API, the CLI or the interface can
    observe has its line in CHANGELOG.md under Unreleased, in the same commit
  - a change to how the system is put together has a record in docs/decisions/
-->

## What this changes

## Why

<!-- Changing DNS protocol behaviour? Name the RFC and section, here or in the commit. -->
